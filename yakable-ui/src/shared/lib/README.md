# Shared Lib

`shared/lib` is for framework-independent or browser-generic helpers that have no product-feature owner.

Examples may include pure formatting, collection, or browser utility functions.

Transport concerns such as HTTP, streams, request errors, and cancellation belong to `shared/api`, not here.
