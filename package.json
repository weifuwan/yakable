{
  "name": "yakable",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently --kill-others -n server,web \"npm run dev --workspace @yakable/server\" \"npm run dev --workspace @yakable/web\"",
    "dev:web": "npm run dev --workspace @yakable/web",
    "dev:server": "npm run dev --workspace @yakable/server",
    "build": "npm run build --workspace @yakable/server && npm run build --workspace @yakable/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
