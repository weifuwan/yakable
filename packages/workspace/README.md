# Workspace

Workspace owns the filesystem boundary for a Yakable project.

Current responsibilities:

- initialize a workspace from a template
- list files
- read files
- write files
- delete files
- prevent paths from escaping the workspace root
