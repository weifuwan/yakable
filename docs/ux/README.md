# UX / Interaction Spec

UX Spec 定义“用户看到什么、怎么操作”。

主要包含：

- 页面布局与区域关系。
- Loading / Empty / Error / Streaming 等界面状态。
- 点击、输入、滚动、快捷键等交互。
- 状态之间的用户可见变化。
- 响应式与可访问性要求。

不包含：

- 后端实现。
- 数据库设计。
- Service / Repository 等代码结构。

原则：**只描述用户界面的行为，不替技术实现做决定。**
