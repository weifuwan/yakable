# Java 通用编程规范

本规范适用于项目中所有 Java 代码。模块专属规范在此基础上补充，不得与本规范冲突。

## 格式

1. 能一行写完就一行；只有明显过长时才换行。换行保持紧凑、统一，禁止无意义换行，禁止把 `) {` 或右括号单独悬空。

## 复用

2. 优先复用项目已有工具、基础类和框架能力。字符串、JSON、对象转换、时间、ID、线程等已有公共能力禁止重复实现。

3. 公共能力优先在当前类使用 `private` 方法收敛；只有形成稳定复用后再抽公共能力，禁止为了少量重复提前抽象。

## 结构

4. 一个类只承担明确职责，禁止无意义增加 Manager、Helper、Handler、Adapter、Base 等中间层。

5. 禁止无意义转发、重复判空、重复转换、重复赋值和无价值的防御性代码。

## Java 使用

6. 能明显减少样板代码时优先使用 Lombok；不可变数据适合 `record` 时优先使用 `record`，不为了使用 Lombok 改变原本清晰的结构。

7. 框架或标准库已经解决的问题优先直接使用，禁止重复封装或手写同类能力。

## 注释

8. 公共接口、公共类和核心方法写必要注释，只说明职责、边界、约束和为什么；接口已有注释，实现类不重复。

9. 简单代码不强制注释；复杂、兼容性或非直观逻辑必须说明原因，过期注释必须删除。

## 原则

**简单、紧凑、统一。**

**优先复用，不重复造轮子。**

**没有真实问题，不提前抽象。**
