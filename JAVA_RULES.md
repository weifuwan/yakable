# Java Rules

Scope:
- Yakable 所有 Java 生产代码
- Yakable 所有 Java 测试代码

Load With:
- 当前模块最近的 `*_RULES.md`
- 修改行为或测试时加载 `BACKEND_TEST_RULES.md`

Principles:
- 简单
- 紧凑
- 统一
- 先复用，后抽象

## Must

- 能一行清晰表达就保持一行；只有明显过长时换行，换行保持紧凑统一。
- 字符串、JSON、对象转换、时间、ID、线程等能力优先复用现有公共实现。
- 当前类能用 `private` 方法收敛时，不提前抽公共层。
- 一个类只承担明确职责。
- Lombok 能明显减少样板代码时优先使用。
- 不可变数据适合 `record` 且更清晰时优先使用 `record`。
- JDK 或框架已经提供的能力直接使用，不重复封装。
- 公共接口、公共类和核心方法只注释职责、边界、约束和非直观原因。
- 注释必须和当前行为一致，过期注释及时删除。

## Must Not

- 没有真实边界时新增 Manager / Helper / Handler / Adapter / Base。
- 增加无意义转发、重复判空、重复转换、重复赋值或没有明确失败场景的防御代码。
- 重复实现已有框架或公共工具能力。
- 写只重复代码含义的注释。
- 为了使用 Lombok 或某种抽象而破坏原本清晰的结构。

## Boundary

模块 RULES 可以增加更具体约束，但不能削弱本文件的全局规则。
