/**
 * Application use cases and ports.
 *
 * <p>This layer coordinates domain objects and defines outbound ports. It must
 * not know HTTP, Spring configuration, concrete persistence adapters, or model
 * provider implementations.</p>
 */
package io.yakable.application;
