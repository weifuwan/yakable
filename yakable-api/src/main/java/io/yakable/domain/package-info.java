/**
 * Framework-independent business concepts and invariants.
 *
 * <p>This module must not depend on Spring, HTTP, persistence implementations,
 * model provider SDKs, or asynchronous execution frameworks.</p>
 */
package io.yakable.domain;
